import Cards from "../components/latest/cards";
import Hero from "../components/latest/hero";
import Layout from "../layout/layout";

const Latest = () => {
  return (
    <Layout>
      <Hero />
      <Cards />
    </Layout>
  );
};

export default Latest;
